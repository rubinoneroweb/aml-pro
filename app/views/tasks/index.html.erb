<p id="notice"><%= notice %></p>

<div class="row">
  <div class="small-12 columns">
    <h1>Elenco Task</h1>


<div class="row">
  <div class="small-12 columns">
<table>
  <thead>
    <tr>
      <th>Operatore</th>
      <th>Lavorazione</th>
      <th>Commessa</th>
      <th>Quantità</th>
      <th>Velocità</th>
      <th>Completato</th>
      <th colspan="3"> - </th>
      
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.operator.name %></td>
        <td><%= task.metalwork.description %></td>
        <td><%= task.order.label_print %></td>
        <td><%= task.quantity %></td>
        <td><%= task.speed %></td>
        <td><%= task.done %></td>
        <td><%= link_to 'Dettagli', task, :class=>"button large" %></td>
        <td><%= link_to 'Modifica', edit_task_path(task), :class=>"button large" %></td>
        <td><%= link_to 'Elimina', task, method: :delete, data: { confirm: 'Sei sicuro?' }, :class=>"button large"%></td>
      </tr>
    <% end %>
  </tbody>
</table>


  </div>
</div>

<div class="row">
  <div class="small-12 columns">
    <%= link_to 'Nuovo Task', new_task_path, :class=>"button extend" %>
  </div>
</div>

