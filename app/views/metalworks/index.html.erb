<p id="notice"><%= notice %></p>
<div class="row">
<h1>Elenco Processi</h1>
</div>

<div class="row">
<table>
  <thead>
    <tr>
      <th>Codice</th>
      <th>Descrizione</th>
      <th>Velocità</th>
      <th>Macchinario</th>
      <th>Strumento</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @metalworks.each do |metalwork| %>
      <tr>
        <td><%= metalwork.name %></td>
        <td><%= metalwork.description %></td>
        <td><%= metalwork.speed if metalwork.speed.present? %></td>
        <td><%= metalwork.machine.name if metalwork.machine.present? %></td>
        <td><%= metalwork.tool.name if metalwork.tool.present? %></td>
      
        <td><%= link_to 'Modifica', edit_metalwork_path(metalwork), :class=>"warning button" %></td>
        <td><%= link_to 'Elimina', metalwork, method: :delete, data: { confirm: 'Sei sicuro?' }, :class=>"alert button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>


</div>

<div class="row">

<%= link_to 'Nuovo processo', new_metalwork_path, :class=>"button" %>
</div>